node_modules/
dist/
build/
.next/
.env
.env.*
!.env.example
*.log
.turbo/
archivos-fuente/
# Documentos subidos al RAG: contenido real de PDVSA, como archivos-fuente/.
datos-rag/

# Clon del repo de skills de terceros (addyosmani/agent-skills): trae su
# propio .git, no es fuente de este proyecto.
agent-skills/

# Ajustes locales de Claude Code: son de la máquina, no del proyecto.
.claude/settings.local.json

# Herramientas locales que no son parte del proyecto: la salida de /graphify,
# los reportes de la crítica de diseño y las skills instaladas en esta máquina
# (la de impeccable y los agentes). Las demás skills de .agents/skills/ y el
# sidecar .impeccable/design.json sí se siguen: por eso se ignoran estas rutas
# puntuales y no las carpetas enteras.
graphify-out/
.impeccable/critique/
.agents/skills/impeccable/
.claude/agents/
.claude/skills/impeccable/
.codex/
